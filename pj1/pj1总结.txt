梯度爆炸坏处：
梯度爆炸意味着梯度值变得非常大，导致参数（权重）每次更新的幅度也非常大。
损失函数会剧烈震荡，模型根本学不到有用的东西
梯度过大，反向传播时可能出现浮点数溢出，最终导致 loss 变成 NaN 或 Inf，模型完全训练不了。
爆炸的梯度可能导致模型对某些样本的权重特别敏感，从而对这些样本“死记硬背”，泛化能力极差。
lr更新为什么不用显式写出-：因为逆着梯度更新的负号与偏导中的负号抵销了

不同的局部最小值对应不同的表征

